package org.example.model.enums;

/**
 * Enum untuk tombol keyboard yang digunakan dalam game
 */
public enum Key {
    W, // Gerak atas
    A, // Gerak kiri
    S, // Gerak bawah
    D, // Gerak kanan
    F, // Pick/drop
    V, // Interact item
    P, // Pause
    TAB // Ganti chef
}
